These test-only fixtures describe the expected `ppc-inspect` output for the
synthetic PEF assembled in `tests/ppc_inspect_cli.rs`. They contain no data
captured from third-party applications.

Update the expected JSON alongside intentional changes to the inspector's
output schema or the synthetic input.
